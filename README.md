# ABNER FLORES

## Webpage design

- Heading: `abner flores?` in blue
- Background: midnight blue
- Birthday: `may 21st, 2012` in black

The finished design is implemented in `index.html`, so the preview displays the
styled webpage instead of the HTML and CSS source code.